pipeline {
    agent any

    stages {

        stage('Build project') {

            steps {
                sh 'mkdir build && cd build && cmake .. && make'
            }
        }

        stage('Test code') {
         
            steps {
                sh '../tests/unit_tests'
            }
        }

        stage('Deploy code') {
         
            steps {
                sh 'echo Test done.'
            }
        }
    }
}