pipeline {
    agent any

    stages {

        stage('Build project') {

            steps {
                cmakeBuild generator: 'Unix Makefiles', buildDir: 'build', buildType: 'Debug', sourceDir: 'APCPPLib/lib/src', installation: 'InSearchPath'
            }
        }
    }
}