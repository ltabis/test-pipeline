pipeline {
    agent any

    stages {
        stage('Build project') {
            steps {
                sh 'cd APCPPLib/'
                sh 'ls -l'
                cmake installation: 'InSearchPath', arguments: '-B build .'
            }
        }
    }
}